1. servo control function using Adafruit circuit python PCA9685 library to take two parameters which are servo channel and servo position
2. using the above function create a scanning function where the x axis is servo 0 and the Y axis is servo 1
3. adding to this function I want to read the output of a qmc5883l sensor using the adafruit circuit python library qmc5883l at every position step
4. adding to the above function the readings from VL6180X sensor using the CircuitPython library module for the VL6180X distance sensor at each servo step.
